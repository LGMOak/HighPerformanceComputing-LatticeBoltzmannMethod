#include <iostream>
#include <vector>
#include <cmath>



int main() {
    // Channel dimensions
    const int nx = 200;
    const int ny = 50;

    // Relaxation time (>0.5)
    const double tau = 1.0;

    // Constant external force
    const double force_x = 1e-5;
    const double force_y = 0.0;

    // number of simulation iterations
    const int num_timesteps = 10000;

}